const List = Object.create(null);

const sequence = (n) => {
    let new_array = Array;
    let zero_array = new_array(n).fill()
    let final_array = zero_array.map((ignore, k) => k)
    return final_array
}


const sequence_new 
const transpose = (array) => array

export default Object.freeze(List); 